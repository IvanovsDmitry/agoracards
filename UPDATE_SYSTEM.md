# Система обновления базы вопросов

## 📝 Формат вопросов

Теперь каждая карта может содержать:
1. **Основной вопрос** (mainQuestion) - главный вопрос на лицевой стороне
2. **Альтернативный вопрос** (additionalQuestion) - вопрос на оборотной стороне
3. **Блок "или то, или то"** (alternatives) - опциональные варианты для облегчения ответа

### Пример с блоком "или то, или то":

```javascript
["Какую суперсилу ты хочешь?", "Как бы ты использовал её в повседневной жизни?", "Телепортация или читать мысли?"]
```

## 🔧 Как обновлять вопросы

### 1. Добавить альтернативы к существующему вопросу

Отредактируйте файл `data.js`, найдите нужный вопрос и добавьте третий элемент в массив:

**Было:**
```javascript
["Какую суперсилу ты хочешь?", "Как бы ты использовал её в повседневной жизни?"]
```

**Стало:**
```javascript
["Какую суперсилу ты хочешь?", "Как бы ты использовал её в повседневной жизни?", "Телепортация или читать мысли?"]
```

### 2. Добавить новый вопрос в колоду

Найдите нужную колоду в `data.js` и добавьте новый элемент в массив `cardsArray`:

```javascript
const friendsDeck = createDeck(
    "Компания людей",
    "🏺",
    "#FF6B6B",
    [
        // ... существующие вопросы
        ["Новый вопрос?", "Альтернативный вопрос?", "Вариант А или вариант Б?"],
    ]
);
```

### 3. Удалить вопрос

Просто удалите строку с вопросом из массива карт в `data.js`.

### 4. Изменить существующий вопрос

Найдите строку с вопросом и измените её:

**Было:**
```javascript
["Старый вопрос?", "Старый альтернативный вопрос?"]
```

**Стало:**
```javascript
["Новый вопрос?", "Новый альтернативный вопрос?", "Новые альтернативы?"]
```

### 5. Обновить название колоды

Найдите определение колоды и измените первый параметр `createDeck()`:

```javascript
const friendsDeck = createDeck(
    "Новое название колоды", // ← измените здесь
    "🏺",
    "#FF6B6B",
    // ...
);
```

### 6. Изменить эмодзи колоды

Измените второй параметр `createDeck()`:

```javascript
const friendsDeck = createDeck(
    "Компания людей",
    "🆕", // ← измените здесь
    "#FF6B6B",
    // ...
);
```

## 📋 Структура отображения

Блок "или то, или то" будет отображаться на оборотной стороне карты после альтернативного вопроса:

```
[Альтернативный вопрос]

┌─────────────────────────┐
│ или то, или то:         │
│ Телепортация или        │
│ читать мысли?           │
└─────────────────────────┘
```

## 🚀 После изменений

1. Сохраните файл `data.js`
2. Обновите страницу в браузере
3. Проверьте изменения

## 📝 Примеры использования

### Пример 1: Простой вопрос без альтернатив
```javascript
["Какой фильм ты бы пересмотрел?", "Что в нём тебя больше всего поразило?"]
```

### Пример 2: Вопрос с альтернативами
```javascript
["Ты бы предпочёл быть невидимым или уметь летать?", "Какие приключения ты бы устроил?", "Невидимость или полёт?"]
```

### Пример 3: Вопрос "Вопросы вечности" с подсказкой
```javascript
["Что такое счастье?", "💭 Подсказка: ...\n\n«Цитата философа»\n\nУточняющий вопрос: ..."]
```

## ⚠️ Важно

- Все изменения нужно делать в файле `data.js`
- После изменений приложение автоматически подхватит новые данные
- Блок "или то, или то" опционален - можно добавлять только к нужным вопросам
- Формат поддерживает обратную совместимость - старые вопросы без альтернатив будут работать


