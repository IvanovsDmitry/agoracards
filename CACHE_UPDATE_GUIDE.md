# Руководство по обновлению кэша мини-приложения в Telegram

## 🔄 Автоматическое обновление

Приложение автоматически обновляет кэш при изменении версии. Версия обновляется в файлах:
- `index.html` - query параметры `?v=X` для всех файлов
- `app.js` - константа `APP_VERSION`

При изменении версии приложение автоматически:
1. Проверяет сохраненную версию в `localStorage`
2. Если версия изменилась - принудительно перезагружает страницу с очисткой кэша
3. Для Telegram WebApp использует более агрессивную очистку кэша

---

## 📱 Для пользователей Telegram мини-приложения

### Способ 1: Закрыть и открыть заново (рекомендуется)
1. Закройте мини-приложение полностью
2. Откройте его заново через бота
3. Кэш обновится автоматически

### Способ 2: Принудительное обновление через меню Telegram
1. В мини-приложении нажмите на меню (три точки) в правом верхнем углу
2. Выберите "Обновить" или "Refresh"
3. Приложение перезагрузится с очисткой кэша

### Способ 3: Очистка через консоль (для разработчиков)
1. Откройте мини-приложение в Telegram
2. Нажмите на меню → "Открыть в браузере" (если доступно)
3. Откройте консоль разработчика (F12)
4. Выполните:
   ```javascript
   localStorage.clear();
   location.reload(true);
   ```

### Способ 4: Удаление и переустановка мини-приложения
1. В настройках Telegram → "Данные и хранилище"
2. Найдите ваше мини-приложение
3. Очистите данные приложения
4. Откройте мини-приложение заново

---

## 🛠️ Для разработчиков: как обновить версию

### Шаг 1: Обновить версию в файлах

**index.html:**
```html
<link rel="stylesheet" href="styles.css?v=32">
<script src="models.js?v=32"></script>
<script src="data.js?v=32"></script>
<script src="deck-manager.js?v=32"></script>
<script src="app.js?v=32"></script>
```

**app.js:**
```javascript
const APP_VERSION = '32';
```

### Шаг 2: Закоммитить и запушить изменения
```bash
git add index.html app.js
git commit -m "Обновить версию кэша до v32"
git push origin main
```

### Шаг 3: После деплоя
- Пользователи автоматически получат обновление при следующем открытии
- Для принудительного обновления можно попросить пользователей закрыть и открыть приложение заново

---

## 🔍 Проверка текущей версии

Чтобы проверить, какая версия загружена у пользователя:

1. Откройте консоль разработчика (F12)
2. Выполните:
   ```javascript
   console.log('App version:', localStorage.getItem('app_version'));
   console.log('Expected version:', '32'); // Замените на актуальную версию
   ```

---

## ⚠️ Важные замечания

1. **Telegram кэширует мини-приложения агрессивно** - иногда нужно полностью закрыть и открыть приложение
2. **Версионирование файлов** (`?v=X`) помогает, но не всегда достаточно для Telegram
3. **localStorage очистка** работает только после перезагрузки страницы
4. **Мета-теги** (`Cache-Control`, `Pragma`, `Expires`) помогают, но Telegram может их игнорировать

---

## 🚀 Рекомендации для быстрого обновления

1. **Всегда увеличивайте версию** при любых изменениях в коде
2. **Используйте уникальные query параметры** для каждого файла
3. **Добавляйте timestamp** при необходимости: `?v=32&_=1234567890`
4. **Информируйте пользователей** о необходимости обновления через уведомления или сообщения в боте

---

**Последнее обновление:** 16.12.2025  
**Текущая версия:** v32
