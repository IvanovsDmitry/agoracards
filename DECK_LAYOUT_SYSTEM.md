# Система верстки оборота карт для колод

## 📋 Обзор

Приложение использует систему типизации колод по верстке оборота карт. Каждый тип колоды имеет свою уникальную верстку, которая обрабатывается отдельной функцией.

**ВАЖНО:** При изменении верстки конкретного типа колоды редактируйте ТОЛЬКО соответствующую функцию `render*Back()`, не затрагивая другие типы!

---

## 🎯 Типы колод

### Тип 1: QUOTE_AND_QUESTION
**Колоды:** "Вопросы вечности", "Атака титанов"

**Верстка оборота:**
- **Верхняя часть** (`main-question-back`): Уточняющий вопрос (обычный шрифт, 20px)
- **Разделитель:** Пунктирная линия (repeating-linear-gradient)
- **Нижняя часть** (`additional-question-back`): Цитата с автором (курсив, 13px)

**Функция обработки:** `renderQuoteAndQuestionBack(card)`

**Особенности:**
- Использует класс `special-deck-back` для специальных стилей
- Цитата имеет курсивный шрифт (`font-style: italic`)
- Размер шрифта цитаты: 13px (меньше обычного)
- Отступы для половинок: `padding: 16px 14px` (уменьшены)

**CSS классы:**
- `.special-deck-back` - основной контейнер
- `.special-deck-back .card-back-bottom .quote-text` - стили для цитаты

---

### Тип 2: QUESTION_AND_HINT
**Колоды:** "Компания людей", "Маленькие люди"

**Верстка оборота:**
- **Верхняя часть** (`additional-question-back`): Дополнительный вопрос или основной вопрос
- **Разделитель:** Пунктирная линия (repeating-linear-gradient)
- **Нижняя часть** (`main-question-back`): Подсказка (alternatives)

**Функция обработки:** `renderQuestionAndHintBack(card)`

**Особенности:**
- Обычный шрифт для обеих частей (20px)
- Стандартные отступы: `padding: 24px 20px`
- Не использует класс `special-deck-back`

---

### Тип 3: NO_FLIP
**Колоды:** "36 вопросов для незнакомцев", "Большая семья"

**Верстка оборота:**
- Оборот карты отключен полностью
- Нет кнопки переворота
- Нет возможности перевернуть карту кликом

**Функция обработки:** `renderNoFlipBack()`

**Особенности:**
- Скрывает все блоки оборота (`card-back-split`, `card-back-simple`)
- Кнопка переворота скрыта через `randomButton.style.display = 'none'`
- Клик на карту для переворота отключен в обработчике событий

**Примечание для "Большая семья":**
- Имеет специальные вводные карты (INTRO_CARD_1-4)
- Вводные карты используют класс `.intro-card` с особыми стилями
- Для вводных карт показывается только текст из `additionalQuestion`

---

## 🏗️ Структура HTML

### Основная структура оборота карты:

```html
<div class="card-back" id="card-back">
    <div class="card-content">
        <!-- Структура для всех колод с оборотом -->
        <div class="card-back-split" id="card-back-split" style="display: none;">
            <!-- Верхняя часть -->
            <div class="card-back-half card-back-top">
                <p class="card-text" id="main-question-back"></p>
            </div>
            
            <!-- Разделитель -->
            <div class="card-back-divider"></div>
            
            <!-- Нижняя часть -->
            <div class="card-back-half card-back-bottom">
                <p class="card-text" id="additional-question-back"></p>
            </div>
        </div>
        
        <!-- Старая структура (для fallback) -->
        <div class="card-back-simple" id="card-back-split">
            <p class="card-text" id="additional-question"></p>
        </div>
    </div>
</div>
```

---

## 🎨 CSS Стили

### Базовые стили для всех типов:

```css
.card-back-split {
    display: flex;
    flex-direction: column;  /* Горизонтальное деление карты */
    width: 100%;
    height: 100%;
    padding: 0;
    justify-content: flex-start;
    overflow: hidden;
    position: relative;
    z-index: 2;
}

.card-back-half {
    flex: 1;  /* Каждая половина занимает 50% высоты */
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 24px 20px;  /* Стандартные отступы */
    min-height: 0;
    box-sizing: border-box;
}

.card-back-divider {
    flex-shrink: 0;
    height: 4px;
    background: repeating-linear-gradient(
        to right,
        rgba(255, 255, 255, 0.7) 0px,
        rgba(255, 255, 255, 0.7) 12px,
        transparent 12px,
        transparent 24px
    );  /* Пунктирная линия */
    margin: 0 20px;
    border: none;
}

.card-back-half .card-text {
    font-size: 20px;
    font-weight: 600;
    color: white;
    text-align: center;
    line-height: 1.4;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
    margin: 0;
    padding: 0 8px;
    word-wrap: break-word;
    overflow-wrap: break-word;
    width: 100%;
}
```

### Специальные стили для QUOTE_AND_QUESTION:

```css
/* Уменьшенные отступы для специальных колод */
.special-deck-back .card-back-half {
    padding: 16px 14px;
}

/* Стили для цитаты (нижняя часть) */
.special-deck-back .card-back-bottom .quote-text {
    font-size: 13px;  /* Меньший размер */
    font-style: italic;  /* Курсив */
    font-weight: 500;
    line-height: 1.35;
    padding: 10px 8px;
    white-space: pre-line;  /* Сохраняет переносы строк */
    text-align: center;
    word-wrap: break-word;
    overflow-wrap: break-word;
    hyphens: auto;
}
```

---

## 🔧 Как редактировать верстку

### ✅ Правильно:
1. Определите тип колоды через `getDeckFlipType(deckName)`
2. Найдите соответствующую функцию `render*Back()`
3. Редактируйте ТОЛЬКО эту функцию
4. Если нужно изменить CSS, обновите соответствующие стили для этого типа

### ❌ Неправильно:
1. ❌ Редактировать общую логику в `updateCardViewer()` без проверки типа
2. ❌ Изменять стили, которые влияют на все типы колод
3. ❌ Менять структуру HTML без учета типов колод
4. ❌ Добавлять проверки на конкретные названия колод вместо использования системы типов

---

## 📝 Примеры редактирования

### Пример 1: Изменить размер шрифта цитаты в QUOTE_AND_QUESTION

**Файл:** `styles.css`
```css
.special-deck-back .card-back-bottom .quote-text {
    font-size: 14px;  /* Изменить с 13px на 14px */
    /* остальные стили без изменений */
}
```

### Пример 2: Добавить новую колоду типа QUOTE_AND_QUESTION

**Файл:** `app.js`
```javascript
const DECK_FLIP_TYPES = {
    QUOTE_AND_QUESTION: ['Вопросы вечности', 'Атака титанов', 'Новая колода'],
    // ...
};
```

### Пример 3: Изменить отступы для QUESTION_AND_HINT

**Файл:** `styles.css`
```css
/* Добавить специальные стили только для этого типа */
.question-and-hint-deck .card-back-half {
    padding: 20px 18px;  /* Новые отступы */
}
```

**Файл:** `app.js` (в функции `renderQuestionAndHintBack`)
```javascript
if (cardBackSplit) {
    cardBackSplit.classList.add('question-and-hint-deck');  // Добавить класс
    // ...
}
```

---

## 🗂️ Файлы системы

- **`app.js`** - Определение типов (`DECK_FLIP_TYPES`), функции рендеринга (`render*Back()`)
- **`styles.css`** - CSS стили для всех типов верстки
- **`index.html`** - HTML структура оборота карты

---

## 📌 Текущее состояние (версия v28)

**Дата фиксации:** 16.12.2025

**Коммит:** `78b1a3a` - "Поменять местами цитаты и подсказки для колод 'Вопросы вечности' и 'Атака титанов'"

**Статус:**
- ✅ Система типизации работает корректно
- ✅ Верстка для всех типов восстановлена из коммита 8776eb5
- ✅ Цитаты и подсказки поменяны местами для QUOTE_AND_QUESTION
- ✅ Стили для цитат настроены (курсив, 13px)

---

## ⚠️ Важные напоминания

1. **Всегда проверяйте тип колоды** перед редактированием верстки
2. **Используйте функции `render*Back()`** вместо прямого редактирования DOM
3. **Тестируйте изменения** на всех типах колод, не только на той, которую редактируете
4. **Обновляйте эту документацию** при внесении изменений в систему верстки

---

## 🔍 Поиск проблем

Если верстка сломалась:

1. Проверьте, какой тип колоды используется: `getDeckFlipType(deckName)`
2. Найдите соответствующую функцию `render*Back()` в `app.js`
3. Проверьте CSS стили для этого типа в `styles.css`
4. Убедитесь, что классы применяются правильно (особенно `special-deck-back`)
5. Проверьте структуру HTML в `index.html`

---

**Последнее обновление:** 16.12.2025
